"""A.I.Z.E.N. — Local Personal AI Operating Environment (Python core).

The LLM is a small, replaceable language layer; code does the real work:
routing, retrieval, math, permissions. The model is never the trust boundary.
"""

__version__ = "0.1.0"
